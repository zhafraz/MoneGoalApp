<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izinkan kamera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Beri tahu sistem bahwa kamera dipakai tapi tidak wajib (agar bisa diinstal di device tanpa kamera) -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoneGoal"
        tools:targetApi="31">

        <activity
            android:name=".ScannerActivity"
            android:exported="false" />

        <activity
            android:name=".RiwayatTransaksiActivity"
            android:exported="false" />

        <activity
            android:name=".TopupActivity"
            android:exported="false" />

        <activity
            android:name=".DetailAjuanAnakActivity"
            android:exported="false" />

        <activity
            android:name=".HomeOrtuActivity"
            android:exported="false" />

        <activity
            android:name=".LoginOrtuActivity"
            android:exported="false" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>